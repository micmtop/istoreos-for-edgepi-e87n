name: E87N Build

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout build repo
        uses: actions/checkout@v4
        with:
          path: build

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang flex bison g++ gawk \
            gcc-multilib g++-multilib gettext git libncurses-dev libssl-dev \
            python3-distutils python3-setuptools rsync unzip zlib1g-dev \
            file wget qemu-utils libelf-dev jq ccache
          sudo apt-get clean
          ccache --max-size 4G
          ccache --set-config=compression=true
          ccache --set-config=compression_level=1
          echo "ccache: $(ccache --version | head -1)"
          # IMPORTANT: do NOT try to point CCACHE_DIR here. iStoreOS/OpenWrt
          # rules.mk hard-assigns + exports CCACHE_DIR to $(TOPDIR)/.ccache
          # (build/istoreos/.ccache) unless CONFIG_CCACHE_DIR is set, so any
          # env override from this workflow is clobbered the moment make runs.
          # The actions/cache step below therefore caches build/istoreos/.ccache
          # directly (same path pattern as the dl/toolchain caches).
          echo "=== disk after deps ==="
          df -h

      - name: Clone iStoreOS 24.10 source
        run: |
          cd build
          rm -rf istoreos
          git clone -b istoreos-24.10 --depth 1 https://github.com/istoreos/istoreos.git istoreos
          cd istoreos
          # iStoreOS stock feeds.conf lists packages/luci/store/third/routing/telephony.
          # The network-wizard + homepage UI (luci-app-quickstart / quickstart) live in
          # Linkease's NAS feeds, which are NOT in the stock feeds.conf. Add them so
          # the E87N build can preinstall quickstart (wizard+homepage) and hnat.
          # NOTE: 'main' does not exist on nas-packages; use 'linkeasefull'.
          # (src-git, not src-gitfull: src-gitfull is an immortalwrt extension that
          #  stock OpenWrt 24.10 rejects with "Unknown type"; printf used because a
          #  YAML heredoc EOF at column 0 breaks the workflow file)
          printf 'src-git nas https://github.com/linkease/nas-packages.git;linkeasefull\nsrc-git nas_luci https://github.com/linkease/nas-packages-luci.git;main\n' >> feeds.conf.default
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Cache OpenWrt downloads (dl)
        # IMPORTANT: must run AFTER "Clone iStoreOS", because the clone step
        # does `rm -rf istoreos` which would delete the restored dl dir.
        uses: actions/cache@v4
        with:
          path: build/istoreos/dl
          key: ${{ runner.os }}-dl-${{ hashFiles('build/e87n.config') }}
          restore-keys: |
            ${{ runner.os }}-dl-

      - name: Cache OpenWrt toolchain (skip gcc/binutils/musl rebuild)
        # IMPORTANT: must run AFTER "Clone iStoreOS", because the clone step
        # does `rm -rf istoreos` which would delete the restored toolchain dirs.
        # Free 2-core runner spends ~20-30min rebuilding the cross toolchain
        # every run. Cache staging_dir/toolchain-* + build_dir/toolchain-*
        # so make sees the .built marker and skips it. Key on the config hash
        # (target arch only changes when e87n.config changes); restore-keys
        # still reuse the last toolchain even when the config did change.
        uses: actions/cache@v4
        with:
          path: |
            build/istoreos/staging_dir/toolchain-*
            build/istoreos/build_dir/toolchain-*
          key: ${{ runner.os }}-toolchain-${{ hashFiles('build/e87n.config') }}
          restore-keys: |
            ${{ runner.os }}-toolchain-

      - name: Cache ccache (kernel + package compile objects)
        # IMPORTANT: cache build/istoreos/.ccache, NOT ~/.cache/ccache. Even
        # though this workflow used to export CCACHE_DIR=$HOME/.cache/ccache,
        # iStoreOS rules.mk re-assigns+exports CCACHE_DIR=$(TOPDIR)/.ccache on
        # every make invocation (unless CONFIG_CCACHE_DIR is configured), so
        # all ccache objects land in build/istoreos/.ccache. Caching the wrong
        # path meant: restore always said "Cache not found", and the Post save
        # had no real data to upload -> no Linux-ccache entry ever appeared.
        # Key MUST NOT depend on build/files/** or apply.sh (edited on every
        # fix; they would invalidate the WHOLE cache each commit). ccache is
        # content-addressed, so a stable key + restore-keys prefix is right.
        uses: actions/cache@v4
        with:
          path: build/istoreos/.ccache
          key: ${{ runner.os }}-ccache-v1
          restore-keys: |
            ${{ runner.os }}-ccache-

      - name: Apply E87N + MT7987 support
        run: bash build/apply.sh

      - name: Configure target (EdgePi E87N)
        run: |
          cd build/istoreos
          cp ../e87n.config .config
          make defconfig
          echo "=== device symbols after defconfig ==="
          grep -E '^CONFIG_TARGET_mediatek_filogic_DEVICE_' .config || true
          echo "=== E87N device check ==="
          if grep -q '^CONFIG_TARGET_mediatek_filogic_DEVICE_edgepi_e87n=y' .config; then
            echo "E87N DEVICE ENABLED"
          else
            echo "FATAL: edgepi_e87n device NOT enabled after defconfig"
            echo "check apply.sh: filogic.mk TARGET_DEVICES += edgepi_e87n and dts files"
            exit 1
          fi

      - name: Show selected packages
        run: |
          cd build/istoreos
          grep -E '^CONFIG_PACKAGE_luci-app-' .config | sort

      - name: Prepare kernel (apply MT7987 patches via quilt)
        run: |
          set -o pipefail
          cd build/istoreos
          make target/linux/prepare V=s 2>&1 | tee /tmp/kernel-prepare.log
          echo "=== quilt log tail ==="
          find build_dir -name "quilt.log" -exec tail -n 40 {} \; 2>/dev/null || true
          if grep -qE "Patch .* does not apply|Failed to apply|Patch failed|Hunk #.* FAILED|FAILED at|saving rejects|quilt.*ERROR|Error:" /tmp/kernel-prepare.log; then
            echo "FATAL: kernel patch application failed"
            exit 1
          fi
          echo "KERNEL PREPARE OK"

      - name: Build firmware
        run: |
          set -o pipefail
          cd build/istoreos
          echo "=== disk before build ==="
          df -h
          # -j2 caps parallelism to avoid OOM/disk-full on the low-memory
          # GitHub runner (run5/run6 failed here).
          # CCACHE=y + cached ccache objects make repeat kernel compiles
          # fast even with -j2 (ccache is content-addressed, so the fresh
          # kernel source still hits cache).
          make CCACHE=y -j2 V=s 2>&1 | tee /tmp/build.log
          echo "=== ccache stats ==="
          ccache -s
          echo "=== disk after build ==="
          df -h

      - name: Extract build errors (always)
        if: always()
        run: |
          echo "=== disk usage ==="
          df -h || true
          du -sh build/istoreos/dl build/istoreos/build_dir build/istoreos/staging_dir 2>/dev/null || true
          if [ -f /tmp/build.log ]; then
            echo "=== last 40 lines of build.log ==="
            tail -n 40 /tmp/build.log || true
            echo "=== matched error lines in build.log ==="
            grep -nEi "no space left|error:|error [0-9]+|fatal|recipe for target|make\[[0-9]+\][^:]*:(error|no rule)|unable to|failed to|killed|out of memory|signal [0-9]" /tmp/build.log | tail -n 120 || true
            grep -nEi "no space left|error:|error [0-9]+|fatal|recipe for target|killed|out of memory" /tmp/build.log | tail -n 120 > /tmp/build-errors.txt || true
          else
            echo "no /tmp/build.log (make may have failed before writing log)"
          fi
          if [ -f /tmp/kernel-prepare.log ]; then
            echo "=== kernel-prepare.log tail ==="
            tail -n 20 /tmp/kernel-prepare.log || true
          fi

      - name: Verify firmware output
        run: |
          cd build/istoreos
          echo "=== filogic dir ==="
          ls -la bin/targets/mediatek/filogic/ 2>/dev/null || echo "NO filogic dir"
          echo "=== find e87n/sysupgrade ==="
          find bin/targets -iname "*e87n*" -ls 2>/dev/null || true
          find bin/targets -iname "*sysupgrade*" -ls 2>/dev/null || true
          if ls bin/targets/mediatek/filogic/*e87n*sysupgrade*.bin >/dev/null 2>&1; then
            echo "FIRMWARE OK"
          else
            echo "FATAL: no E87N sysupgrade image produced"
            exit 1
          fi
          echo "=== kernel .config FB symbols (ground truth) ==="
          KCFG=$(ls build_dir/target-*/linux-mediatek_filogic/linux-6.6.144/.config 2>/dev/null | head -1)
          if [ -n "$KCFG" ]; then
            grep -E '^CONFIG_(STAGING|FB|FB_DEVICE|FB_TFT|FB_TFT_NV3007|SPI|GPIOLIB)=' "$KCFG" || true
            echo "=== hard gate: NV3007 must be enabled and compiled ==="
            NVCFG=$(grep '^CONFIG_FB_TFT_NV3007=' "$KCFG" | head -1)
            if [ "$NVCFG" = "CONFIG_FB_TFT_NV3007=y" ]; then
              echo "NV3007 CONFIG OK (=y)"
            else
              echo "FATAL: CONFIG_FB_TFT_NV3007 != y in kernel .config (current: '$NVCFG')"
              exit 1
            fi
            if ls build_dir/target-*/linux-mediatek_filogic/linux-6.6.144/drivers/staging/fbtft/fb_nv3007.o >/dev/null 2>&1; then
              echo "fb_nv3007.o COMPILED (built-in)"
            else
              echo "FATAL: fb_nv3007.o not found - screen driver NOT built"
              exit 1
            fi
          else
            echo "FATAL: kernel .config not found"
            exit 1
          fi

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e87n-istoreos-firmware
          path: |
            build/istoreos/bin/targets/mediatek/filogic/*.bin
            build/istoreos/bin/targets/mediatek/filogic/*.itb
            build/istoreos/bin/targets/mediatek/filogic/*sysupgrade*
            build/istoreos/bin/targets/mediatek/filogic/packages
          if-no-files-found: warn
          retention-days: 14

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: |
            /tmp/build.log
            /tmp/build-errors.txt
            /tmp/kernel-prepare.log
            build/istoreos/logs/
            build/istoreos/build_dir/target-*/linux-*/quilt.log
            build/istoreos/build_dir/target-*/linux-mediatek_filogic/linux-6.6.144/.config
            build/istoreos/build_dir/target-*/linux-mediatek_filogic/linux-6.6.144/.config.set
          retention-days: 7
