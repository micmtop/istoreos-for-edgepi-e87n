name: Build iStoreOS for EdgePi E87N

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout build repo
        uses: actions/checkout@v4
        with:
          path: build

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang flex bison g++ gawk \
            gcc-multilib g++-multilib gettext git libncurses-dev libssl-dev \
            python3-distutils python3-setuptools rsync unzip zlib1g-dev \
            file wget qemu-utils libelf-dev jq

      - name: Clone iStoreOS 24.10 source
        run: |
          git clone -b istoreos-24.10 --depth 1 https://github.com/istoreos/istoreos.git istoreos
          cd istoreos
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Apply E87N + MT7987 support
        run: bash build/apply.sh

      - name: Configure target (EdgePi E87N)
        run: |
          cd istoreos
          cp ../build/e87n.config .config
          make defconfig

      - name: Show selected packages
        run: |
          cd istoreos
          grep -E '^CONFIG_PACKAGE_luci-app-' .config | sort

      - name: Build firmware
        run: |
          cd istoreos
          make -j$(nproc) V=s 2>&1 | tee /tmp/build.log

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e87n-istoreos-firmware
          path: |
            istoreos/bin/targets/mediatek/filogic/*sysupgrade*.bin
            istoreos/bin/targets/mediatek/filogic/*.itb
            istoreos/bin/targets/mediatek/filogic/*.bin
          retention-days: 14
